diff --git a/src/components/ShiftTable.tsx b/src/components/ShiftTable.tsx
index f237fbe..3f03fc1 100644
--- a/src/components/ShiftTable.tsx
+++ b/src/components/ShiftTable.tsx
@@ -560,6 +560,10 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
     try {
       await deleteShift(shiftId);
       console.log('✅ Firestoreから削除完了:', shiftId);
+
+      // ローカルステートを更新
+      const updatedShifts = shifts.filter(s => s.id !== shiftId);
+      onUpdateShifts(updatedShifts);
     } catch (error) {
       console.error('❌ 削除に失敗しました:', error);
     }
@@ -571,7 +575,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
         menu.remove();
       }
     }
-  }, [updateTotalsForHelperAndDate, undoStackRef]);
+  }, [updateTotalsForHelperAndDate, undoStackRef, shifts, onUpdateShifts]);
 
   // Undo関数
   const undo = useCallback(() => {
@@ -698,7 +702,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
         }
 
         const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);
+        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
         const startTime = timeMatch ? timeMatch[1] : '';
         const endTime = timeMatch ? timeMatch[2] : '';
 
@@ -873,7 +877,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
         }
 
         const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);
+        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
         const startTime = timeMatch ? timeMatch[1] : '';
         const endTime = timeMatch ? timeMatch[2] : '';
 
@@ -1726,7 +1730,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
         }
 
         const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);
+        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
         const startTime = timeMatch ? timeMatch[1] : '';
         const endTime = timeMatch ? timeMatch[2] : '';
 
@@ -1747,11 +1751,16 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
         };
 
         saveShiftWithCorrectYearMonth(shift);
+
+        // ローカルステートを更新
+        const updatedShifts = shifts.filter(s => s.id !== shift.id);
+        updatedShifts.push(shift);
+        onUpdateShifts(updatedShifts);
       }
     }, 100);
 
     console.log('📌 セルにペーストしました');
-  }, [copyBufferRef, updateTotalsForHelperAndDate, year, month]);
+  }, [copyBufferRef, updateTotalsForHelperAndDate, shifts, onUpdateShifts]);
 
   // キーボードイベント（Cmd+C / Cmd+V / Cmd+Z / Cmd+Shift+Z / 直接入力）のリスナー
   useEffect(() => {
@@ -1798,7 +1807,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
           copiedCaresRef.current.forEach((copiedCare) => {
             const newShift: Shift = {
               ...copiedCare.data,
-              id: `${targetCell.helperId}-${targetCell.date}-${targetCell.rowIndex}-${Date.now()}-${Math.random()}`,
+              id: `shift-${targetCell.helperId}-${targetCell.date}-${targetCell.rowIndex}`,
               helperId: targetCell.helperId,
               date: targetCell.date,
               rowIndex: targetCell.rowIndex
@@ -1958,7 +1967,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
                       duration: parseFloat(durationStr) || 0,
                       area,
                       rowIndex: currentRowIndex,
-                      ...(newCancelStatus ? { cancelStatus: newCancelStatus } : {}),
+                      ...(newCancelStatus ? { cancelStatus: newCancelStatus, canceledAt: existingShift?.canceledAt } : {}),
                       regularHours: payCalculation.regularHours,
                       nightHours: payCalculation.nightHours,
                       regularPay: payCalculation.regularPay,
@@ -2119,7 +2128,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
                       duration: parseFloat(durationStr) || 0,
                       area,
                       rowIndex: parseInt(currentRow),
-                      ...(newCancelStatus ? { cancelStatus: newCancelStatus } : {}),
+                      ...(newCancelStatus ? { cancelStatus: newCancelStatus, canceledAt: existingShift?.canceledAt } : {}),
                       regularHours: payCalculation.regularHours,
                       nightHours: payCalculation.nightHours,
                       regularPay: payCalculation.regularPay,
@@ -2739,7 +2748,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
           }
 
           const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-          const timeMatch = timeRange.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);
+          const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
           const startTime = timeMatch ? timeMatch[1] : '';
           const endTime = timeMatch ? timeMatch[2] : '';
 
@@ -2755,7 +2764,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
             area,
             rowIndex: rowIdx,
             cancelStatus: 'keep_time',
-            canceledAt: new Date()
+            canceledAt: new Date() as any // Firestore Timestampに変換される
           };
 
           try {
@@ -2913,7 +2922,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
           }
 
           const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-          const timeMatch = timeRange.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);
+          const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
           const startTime = timeMatch ? timeMatch[1] : '';
           const endTime = timeMatch ? timeMatch[2] : '';
 
@@ -2929,7 +2938,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
             area,
             rowIndex: rowIdx,
             cancelStatus: 'remove_time',
-            canceledAt: new Date()
+            canceledAt: new Date() as any // Firestore Timestampに変換される
           };
 
           try {
@@ -3135,7 +3144,8 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
           const restoredShift: Shift = {
             ...existingShift,
             // cancelStatusを明示的にundefinedに設定（Firestoreから削除される）
-            cancelStatus: undefined
+            cancelStatus: undefined,
+            canceledAt: undefined
           };
 
           // remove_timeの場合、時間情報を復元
@@ -3462,7 +3472,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
           }
         }
         const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);
+        const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
         const startTime = timeMatch ? timeMatch[1] : '';
         const endTime = timeMatch ? timeMatch[2] : '';
 
@@ -3482,11 +3492,16 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
           canceledAt: sourceShift?.canceledAt
         };
         saveShiftWithCorrectYearMonth(shift);
+
+        // ローカルステートを更新
+        const updatedShifts = shifts.filter(s => s.id !== shift.id);
+        updatedShifts.push(shift);
+        onUpdateShifts(updatedShifts);
       }
     }, 10);
 
     setDraggedCell(null);
-  }, [draggedCell, updateTotalsForHelperAndDate, shiftMap]);
+  }, [draggedCell, updateTotalsForHelperAndDate, shifts, onUpdateShifts]);
 
   const getDayHeaderBg = useCallback((dayOfWeekIndex: number) => {
     if (dayOfWeekIndex === 6) return 'bg-blue-200';
@@ -4328,7 +4343,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
                                 }
 
                                 const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-                                const timeMatch = timeRange.match(/(\d{1,2}:\d{2})\s*-\s*(\d{1,2}:\d{2})/);
+                                const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
                                 const startTime = timeMatch ? timeMatch[1] : '';
                                 const endTime = timeMatch ? timeMatch[2] : '';
 
@@ -4351,7 +4366,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
                                   duration: parseFloat(durationStr) || 0,
                                   area,
                                   rowIndex: parseInt(currentRow),
-                                  ...(newCancelStatus ? { cancelStatus: newCancelStatus } : {}),
+                                  ...(newCancelStatus ? { cancelStatus: newCancelStatus, canceledAt: existingShift?.canceledAt } : {}),
                                   regularHours: payCalculation.regularHours,
                                   nightHours: payCalculation.nightHours,
                                   regularPay: payCalculation.regularPay,
@@ -4431,7 +4446,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
                                       }
 
                                       const clientName = clientInfo.replace(/\(.+?\)/, '').trim();
-                                      const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*-\s*(\d{1,2}:\d{2}))?/);
+                                      const timeMatch = timeRange.match(/(\d{1,2}:\d{2})(?:\s*[-~]\s*(\d{1,2}:\d{2}))?/);
                                       const startTime = timeMatch ? timeMatch[1] : '';
                                       const endTime = timeMatch && timeMatch[2] ? timeMatch[2] : '';
 
@@ -4459,7 +4474,7 @@ const ShiftTableComponent = ({ helpers, shifts, year, month, onUpdateShifts }: P
                                         duration: parseFloat(durationStr) || 0,
                                         area,
                                         rowIndex: parseInt(currentRow),
-                                        ...(newCancelStatus ? { cancelStatus: newCancelStatus } : {}),
+                                        ...(newCancelStatus ? { cancelStatus: newCancelStatus, canceledAt: existingShift?.canceledAt } : {}),
                                         regularHours: payCalculation.regularHours,
                                         nightHours: payCalculation.nightHours,
                                         regularPay: payCalculation.regularPay,
